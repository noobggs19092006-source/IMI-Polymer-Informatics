# ── Core scientific stack ─────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0

# ── Cheminformatics ──────────────────────────────────────────────────────────
rdkit>=2023.3

# ── Machine learning extras ──────────────────────────────────────────────────
xgboost>=2.0,<4.0
matplotlib>=3.7,<4.0

# ── API / Web ─────────────────────────────────────────────────────────────────
fastapi>=0.100,<1.0
uvicorn[standard]>=0.23,<1.0
pydantic>=2.0,<3.0
python-multipart>=0.0.9

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66
pyyaml>=6.0,<7.0
chardet>=5.2

# ── Development / CI ─────────────────────────────────────────────────────────
black==23.12.1
flake8==6.1.0
mypy==1.7.1
pytest>=7.4,<9.0
pytest-cov>=4.1

# ── ANSYS integration (optional — install manually on host) ──────────────────
# ansys-aedt-core is NOT pip-installable from PyPI.
# Install via the ANSYS Electronics Desktop installer bundle.
# See README.md for instructions.
